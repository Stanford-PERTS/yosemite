"""Each app must contain, at minimum, a config.py file, and a subprogram, which
is a jinja2 template that extends templates/base_programs.html.

The app may contain a static folder, the contents of which are served as static
resources. Good for things like javascript files.
"""

import config
